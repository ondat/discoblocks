resources:
- config.yaml
- manager.yaml
- service.yaml

generatorOptions:
  disableNameSuffixHash: true

patchesStrategicMerge:
- frps_container_patch.yaml

configMapGenerator:
- files:
  - controller_manager_config.yaml
  name: manager-config
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
images:
- name: controller
  newName: discoblocks
  newTag: latest
